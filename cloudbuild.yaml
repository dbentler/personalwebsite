steps:
  - name: 'gcr.io/cloud-builders/npm'
    args: ['install']
  - name: 'gcr.io/cloud-builders/npm'
    args: ['run', 'build']

artifacts:
  # This deploys the contents of the 'build' directory to App Engine
  deploy:
    - 'app.yaml'
    - 'build/**'
